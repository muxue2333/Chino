@page
@model Chino.IdentityServer.Pages.Account.LogoutModel
@inject IStringLocalizer<Chino.IdentityServer.Pages.Account.LogoutModel> L

@{ 
    ViewData["Nav_ShowLogin/Out"] = false;
}

<div class="logout-page">
    <div class="lead">
        <h1>@L["title_logout"]</h1>
        <p>@L["title_require_logout"]</p>
    </div>

    <form asp-page="Logout">
        @*<input type="hidden" name="logoutId" value="@Model.LogoutId" />*@
        <div class="form-group">
            <button class="btn btn-primary">@L["btn_logout"]</button>
        </div>
    </form>
</div>